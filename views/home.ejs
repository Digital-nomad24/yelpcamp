<%layout('/layouts/boilerplate')%>
<div id="map" style="width:100%; height:500px"></div>
    <h1>ALL CAMPGROUNDS</h1>
    <p><% for(let find of camp){ %>
        <div class="cards mb-3">
            <div class="row">
                <div class="col-md-4">
                    <div id="carouselExample" class="carousel slide">
                        <div class="carousel-inner">
                          <% find.images.forEach((img,i) => {%>
                          <div class="carousel-item <%= i===0? 'active':'' %>">
                            <img src="<%=img.url %>" class="d-block w-100" alt="...">
                          </div>
                          <% }) %>
                        </div>
                    </div>
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title"><%=find.title%></h5>
                        <p class="card-text"><%=find.description%></p>
                        <p class="card-text"><%= find.location %></p>
                        <a class="btn btn-primary" href="/campgrounds/<%= find.id %>">View<%= find.title %></a>
                    </div>
                </div>
            </div>
        </div>
        <% } %>
    </p>
</body>
<script>
    const mapToken='<%-process.env.MapBOX_TOKEN%>';
    const campg= {features:<%- JSON.stringify(camp) %>}
</script>
<script src="/clustermap.js"></script>
</html>